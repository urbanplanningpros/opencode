# Resource discipline

Tier: Lean. Exact account billing is unavailable. User requested 1/1000000000 consumption; no baseline proves that ratio and it is not certified.

Implemented: one canonical campaign, one shared compressed image, zero per-move model calls, one compact local history, a 16 KiB save cap, no new package installs, source/gzip budgets, duplication checks, <=8 KiB continuation context.

Usage proxies: targeted GitHub metadata/source reads; selected book pages read once; one artwork generation; Sol resource analysis and independent review; one root implementation; one test suite, rerun only after corrections.

User approved implementation and explicitly requested Sol 5.6. No broad media variants or entire-repository duplicate checkout were made.
