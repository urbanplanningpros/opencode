# Verification marker

This file exists only to trigger a clean pull-request validation of the Windows atomic-edit continuity patch and should be removed before merge if the branch is merged by squash.
